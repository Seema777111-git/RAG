# Hybrid RAG - runtime dependencies (Python 3.11+)
#
# Install:  python -m pip install -r requirements.txt
#
# Note on PyTorch: sentence-transformers pulls in torch, which is large (the default Linux wheel bundles CUDA).
# For a CPU-only machine, install torch first from the CPU index, then run the command above:
#   python -m pip install torch --index-url https://download.pytorch.org/whl/cpu

# --- API server
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7,<3
pydantic-settings>=2.3,<3

# --- LLM providers (OpenAI is the default; Anthropic is optional, switch with LLM_PROVIDER)
openai>=1.40
langchain-openai>=0.2
anthropic>=0.40
langchain-anthropic>=0.3

# --- Observability
langsmith>=0.2

# --- Guardrails + evaluation (APIs verified against these minor versions)
nemoguardrails>=0.24,<0.25
deepeval>=4.2,<5

# --- Retrieval stack
numpy>=1.26
faiss-cpu>=1.8
networkx>=3.2
pymupdf>=1.24.3
sentence-transformers>=3.0

# --- Frontend
streamlit>=1.40
httpx>=0.27
